// index.js
const animalName = "Cat";
const height = 25;
const isIndoorPet = true;

// Display only the animalName in the element with id="result"
document.getElementById("result").innerHTML = animalName;
